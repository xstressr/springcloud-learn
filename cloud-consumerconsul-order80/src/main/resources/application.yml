server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 172.19.53.133
      port: 8500
      discovery:
        service-name: ${spring.application.name}


